/** @type {import('next').NextConfig} */
const nextConfig = {
  // Look for path references that might need updating
  // Most Next.js configs won't need updates since they use
  // relative paths or aliases that are defined in tsconfig.json
};

module.exports = nextConfig;
